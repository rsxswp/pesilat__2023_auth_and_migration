const { PaketWisata } = require("./../../models");

// tidak menggunakan module-alias supaya bisa langsung menuju file nya
// file ini dibuat supaya mudah diaca dari penempatan file file nya

module.exports = PaketWisata;
